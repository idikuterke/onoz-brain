{"id": "GS-01", "kind": "regression", "title": "38 sinifli kilitli etiket seti (gokturk_labels_v1_locked.json) byte-seviyesinde degismemis", "verify": "python -X utf8 -c \"import hashlib,pathlib,sys; h=hashlib.sha256(pathlib.Path('gokturk_labels_v1_locked.json').read_bytes()).hexdigest(); print('sha256', h); sys.exit(0 if h=='2c6c77b844e0cf7255d70df1ae35df4b866c9a5475c06a35e16910be673985a5' else 1)\"", "timeout": 30, "needs_adjust": false}
{"id": "GS-02", "kind": "regression", "title": "Tum Python kaynak dosyalari (kok + pipeline + scripts + gokturk_pipeline_v2) syntax hatasiz derlenir", "verify": "python -X utf8 -c \"import pathlib; fs=set(pathlib.Path('.').glob('*.py')); [fs.update(pathlib.Path(d).rglob('*.py')) for d in ('pipeline','scripts','gokturk_pipeline_v2')]; fs=sorted(f for f in fs if '__pycache__' not in f.parts); print(len(fs), 'dosya derleniyor'); [compile(f.read_bytes(), str(f), 'exec') for f in fs]\"", "timeout": 60, "needs_adjust": false}
{"id": "GS-03", "kind": "regression", "title": "SpellingEngine kilitli semayla yuklenir ve latin metni class_id dizisine cevirir", "verify": "python -X utf8 -c \"import sys; sys.path.insert(0, 'pipeline/product'); from rules_engine import SpellingEngine; e=SpellingEngine('gokturk_labels_v1_locked.json'); s=e.expected_sequence('tengri'); assert isinstance(s, list) and len(s) > 0, 'expected_sequence bos/gecersiz'; print('OK uzunluk', len(s))\"", "timeout": 60, "needs_adjust": false}
