{"id": "GV-01", "kind": "regression", "title": "Tum Python scriptleri sozluk ve syntax hatasiz derlenir", "verify": "python -c \"import py_compile, pathlib; [py_compile.compile(str(p), doraise=True) for p in pathlib.Path('.').glob('*.py')]\"", "timeout": 60, "needs_adjust": false}
{"id": "GV-02", "kind": "regression", "title": "Girdi manifesti semasi ve zorunlu alanlari dogrulanir", "verify": "python -c \"import json; m=json.load(open('inputs/manifest.json', encoding='utf-8')); assert m['items'] and all(i.get('id') and i.get('prompt') for i in m['items'])\"", "timeout": 30, "needs_adjust": false}
{"id": "GV-03", "kind": "regression", "title": "Workflow dugum yapisi ve baglantilari gecerli", "verify": "python -c \"import json,sys; w=json.load(open('workflows/gokturk_sdxl_controlnet_workflow.json',encoding='utf-8')); ids=set(w); assert all('class_type' in n and 'inputs' in n for n in w.values()), 'dugumde class_type/inputs yok'; refs=[v[0] for n in w.values() for v in n['inputs'].values() if isinstance(v,list) and len(v)==2 and isinstance(v[0],str)]; eksik=[r for r in refs if r not in ids]; sys.exit(1 if eksik else 0)\"", "timeout": 30, "needs_adjust": false}
