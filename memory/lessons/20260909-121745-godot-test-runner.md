- **godot-test-runner**: Godot headless kosusu .import/.tres dosyalarini ayni icerikle yeniden yazar; tam regression sonrasi agac kirli gorunup assert_tree_safe'i tetikleyerek trial start'i engelleyebilir. IKI AYRI SEBEP VAR: (1) satir-sonu -- projede .gitattributes yoksa eol farki gercek diff uretir; cozum metin kaynaklarini (gd/import/tres/tscn/uid) eol=lf'e sabitlemek. (2) bayat stat kaydi -- icerik bit-bit ayni oldugu halde mtime degistigi icin git M gosterir; .gitattributes bunu COZMEZ. Ayirt etme: git diff (ve --text) bos, cmp baytlari esit, git diff-files hedef sha'yi sifir gosteriyorsa sebep (2)'dir. Cozum: git update-index --refresh yetmez, `git add -A` ile stat tazelenir ve icerik esitse hicbir sey stage'lenmez. Trial start oncesi kirlilik gorursen once git diff'e bak, bosaltmadan once neyin degistigini dogrula.
